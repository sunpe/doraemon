[policy.k8s]
allowed_namespaces = ["default", "prod"]
denied_resources = ["secret", "secrets"]
max_log_tail = 500

[policy.docker]
allowed_containers = ["app", "nginx", "worker"]
max_log_tail = 500

[policy.host]
allowed_services = ["nginx", "docker", "kubelet"]
